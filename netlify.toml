[build]
  publish = "build"
  command = "npm run build"

# Proxy MailerLite API to avoid CORS issues
[[redirects]]
  from = "/api/mailerlite/*"
  to = "https://connect.mailerlite.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200